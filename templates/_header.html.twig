<header class="container-fluid">
    <nav class="navbar navbar-expand-lg navbar-light d-flex" >
        <a class="navbar-brand order-2 order-lg-1" href="/">
            <img src="{{ asset('build/images/my-logo.svg') }}" height="60" width="60" />
        </a>
        <div class="navbar-brand text-center font-weight-bolder col-12 col-lg-9 order-1 order-lg-2 text-wrap"
             style="font-size: 50px;"
        >
            GOLGOTH BANK'S
        </div>
        <button
                class="navbar-toggler ml-auto order-3"
                type="button"
                data-toggle="collapse"
                data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent"
                aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse order-3" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto align-items-center">
            {% if is_granted('ROLE_BANKER')
                or is_granted('ROLE_CUSTOMER')
                or is_granted('ROLE_VALIDATED_CUSTOMER')
                or is_granted('ROLE_TO_DELETED_CUSTOMER')
            %}
                {% include '_connected_navbar.html.twig' %}
            {% else %}
                <li class="nav-item border rounded text-center m-1">
                    <a class="nav-link text-dark font-weight-bold" href="/add-customer">
                        <img style="fill: #000000" src="{{ asset('build/images/add-user.svg') }}" height="40" width="40" /><br />
                        Devenir client
                    </a>
                </li>
                <li class="nav-item bg-danger rounded-lg text-center m-1">
                    <a class="nav-link text-light font-weight-bold" href="/login"><img style="fill: #FFFFFF" src="{{ asset('build/images/login.svg') }}" height="40" width="40" /><br />
                        connexion
                    </a>
                </li>
            {% endif %}
            </ul>
        </div>
    </nav>
</header>

