<header class="container-fluid">
    <nav class="navbar navbar-expand-lg navbar-light " >
        <a class="navbar-brand" href="/"><img src="{{ asset('build/images/my-logo.svg') }}" height="60" width="60" /></a>
        <div class="navbar-brand text-center font-weight-bolder col-7 col-xl-9">
            GOLGOTH BANK'S
        </div>
        <button class="navbar-toggler ml-auto" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        {% if is_granted('ROLE_BANKER') or is_granted('ROLE_CUSTOMER') or is_granted('ROLE_VALIDATED_CUSTOMER') %}
            {% include '_connected_navbar.html.twig' %}
        {% else %}
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto align-items-center">
                    <li class="nav-item border rounded text-center m-1">
                        <a class="nav-link text-dark font-weight-bold" href="/add-customer"><img style="fill: #000000" src="{{ asset('build/images/add-user.svg') }}" height="40" width="40" /><br />
                            Devenir client
                        </a>
                    </li>
                    <li class="nav-item bg-danger rounded-lg text-center m-1">
                        <a class="nav-link text-light font-weight-bold" href="/login"><img style="fill: #FFFFFF" src="{{ asset('build/images/login.svg') }}" height="40" width="40" /><br />
                            connexion
                        </a>
                    </li>
                </ul>
            </div>
        {% endif %}
    </nav>
</header>

