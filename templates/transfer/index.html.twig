{% extends 'base.html.twig' %}

{% block title %}Demande de création d'un bénéficiaire{% endblock %}

{% block page_title %}Demande de création d'un bénéficiaire{% endblock %}
{% block body %}
<div class="container">
    {{ form_start(form) }}
    <div class="form-row">
        <div class="form-group col-12">
            {{ form_label(form.beneficiary) }}
            {{ form_widget(form.beneficiary) }}
        </div>
    </div>
    <div class="form-row">
        <div class="form-group col-lg-4">
            {{ form_label(form.reference) }}
            {{ form_widget(form.reference) }}
        </div>
        <div class="form-group col-lg-8">
            {{ form_label(form.label) }}
            {{ form_widget(form.label) }}
        </div>
    </div>
    <div class="form-row">
        <div class="form-group col-12">
            {{ form_label(form.amount) }}{{ form_widget(form.amount) }}
        </div>
    </div>
    <input type="submit" class="btn btn-outline-primary col-12" value="Valider" >
    {{ form_end(form) }}
{% endblock %}
