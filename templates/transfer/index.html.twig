{% extends 'base.html.twig' %}

{% block title %}Viremment{% endblock %}

{% block page_title %}Effectué un virement{% endblock %}
{% block body %}
<div class="container">
    {{ form_start(form) }}
    <div class="form-row">
        <div class="form-group col-12">
            {{ form_label(form.beneficiary) }}
            {{ form_widget(form.beneficiary) }}
        </div>
    </div>
    <div class="form-row">
        <div class="form-group col-lg-4">
            {{ form_label(form.reference) }}
            {{ form_widget(form.reference) }}
        </div>
        <div class="form-group col-lg-8">
            {{ form_label(form.label) }}
            {{ form_widget(form.label) }}
        </div>
    </div>
    <div class="form-row">
        <div class="form-group col-12">
            {{ form_label(form.amount) }}{{ form_widget(form.amount) }}
        </div>
    </div>
    <div class="form-row">
        <div class="container  text-center">
            <input type="submit" class="btn btn-outline-primary col-6" value="Valider" >
        </div>
    </div>

    {{ form_end(form) }}
{% endblock %}